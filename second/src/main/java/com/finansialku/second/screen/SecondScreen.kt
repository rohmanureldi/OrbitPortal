package com.finansialku.second.screen

import com.finansialku.core.ui.ActivityScreen
import com.finansialku.second.R

enum class SecondScreen(override val navId: Int): ActivityScreen {
    SECOND_SCREEN(R.id.secondFragment),
    SECOND_ONE_SCREEN(R.id.secondOneFragment)
}