package com.finansialku.first.navigation.screen

import com.finansialku.core.ui.ActivityScreen
import com.finansialku.first.R

enum class FirstActivityScreen(override val navId: Int): ActivityScreen {
    FIRST(R.id.firstFragment)
}